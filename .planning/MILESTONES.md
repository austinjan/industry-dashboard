# Milestones

## Completed

### v0.0.1 — Foundation & Core Features
- Foundation (auth, RBAC, audit, site/line/machine CRUD)
- Traditional dashboards (alerts, machine status, data visualization)
- Custom dashboards (widget system, drag-and-drop builder)
- LLM integration (dashboard-cli, API key auth)
- Deployment infrastructure (Docker, embedded frontend, cross-compilation)
- i18n (EN, zh-TW, th, vi)

### v0.0.2 — Deploy & Workers
- Deploy improvements (.env support, simplified setup, docker-compose production)
- Modbus workers (data ingestion, coordinator, fake/real datasources)
- Worker Config Builder UI (YAML generation, admin pages)

## Current

### v0.0.3 — Local Auth, Error Handling & Release Validation
- Local authentication (email/password registration and login)
- Self-registration with guest role
- Optional Microsoft SSO binding to local accounts
- Error handling review
- End-to-end release validation
