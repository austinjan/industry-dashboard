---
phase: 03-frontend-auth-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/zh-TW.json
  - frontend/src/locales/th.json
  - frontend/src/locales/vi.json
autonomous: true
requirements: [ERR-02]

must_haves:
  truths:
    - "apiFetch does not trigger refresh-token retry when login or register returns 401"
    - "All auth form labels and error messages have i18n keys in all 4 locales"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "401 interceptor with auth endpoint exemption"
      contains: "/auth/login/local"
    - path: "frontend/src/locales/en.json"
      provides: "English auth form and error i18n keys"
      contains: "auth.emailLabel"
    - path: "frontend/src/locales/zh-TW.json"
      provides: "Traditional Chinese auth translations"
      contains: "auth.emailLabel"
    - path: "frontend/src/locales/th.json"
      provides: "Thai auth translations"
      contains: "auth.emailLabel"
    - path: "frontend/src/locales/vi.json"
      provides: "Vietnamese auth translations"
      contains: "auth.emailLabel"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "login/register pages"
      via: "path check skipping refresh on auth endpoints"
      pattern: "auth/login/local|auth/register"
---

<objective>
Fix the apiFetch 401 interceptor so auth endpoint failures don't trigger token refresh loops, and add all i18n keys needed by the login and registration forms.

Purpose: Foundation for Plan 02 — the login and register pages need the interceptor fix to work correctly and the i18n keys to render translatable text.
Output: Updated api.ts with path-based 401 exemption, all 4 locale JSON files with auth form/error keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-auth-ui/03-CONTEXT.md

@frontend/src/lib/api.ts
@frontend/src/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix apiFetch 401 interceptor to skip refresh on auth endpoints</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>frontend/src/lib/api.ts</read_first>
  <action>
Modify the apiFetch function to skip the refresh-token retry when the request path is an auth credential endpoint (per D-13).

Add a path-based check before the 401 refresh block. Define an array of auth paths that should NOT trigger refresh:

```typescript
const AUTH_NO_REFRESH_PATHS = ['/auth/login/local', '/auth/register'];
```

In the 401 handler, check if `path` starts with any of these prefixes. If it does, return the original response immediately without attempting refresh.

The updated logic:

```typescript
export async function apiFetch(path: string, options: RequestInit = {}): Promise<Response> {
  let res = await fetch(`${API_BASE}${path}`, {
    ...options,
    credentials: 'include',
  });

  // Do NOT retry auth credential endpoints — a 401 here means bad credentials, not expired token
  const skipRefresh = AUTH_NO_REFRESH_PATHS.some(p => path.startsWith(p));

  if (res.status === 401 && !skipRefresh) {
    const refreshRes = await fetch(`${API_BASE}/auth/refresh`, {
      method: 'POST',
      credentials: 'include',
    });
    if (refreshRes.ok) {
      res = await fetch(`${API_BASE}${path}`, {
        ...options,
        credentials: 'include',
      });
    }
  }

  return res;
}
```
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard/frontend && grep -q "AUTH_NO_REFRESH_PATHS" src/lib/api.ts && grep -q "skipRefresh" src/lib/api.ts && grep -q "auth/login/local" src/lib/api.ts && grep -q "auth/register" src/lib/api.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - api.ts contains `const AUTH_NO_REFRESH_PATHS = ['/auth/login/local', '/auth/register']`
    - api.ts contains `const skipRefresh = AUTH_NO_REFRESH_PATHS.some(p => path.startsWith(p))`
    - api.ts contains `res.status === 401 && !skipRefresh` condition
    - The function signature `export async function apiFetch(path: string, options: RequestInit = {}): Promise<Response>` is unchanged
  </acceptance_criteria>
  <done>apiFetch returns 401 responses from login/register endpoints directly without attempting token refresh</done>
</task>

<task type="auto">
  <name>Task 2: Add auth form and error i18n keys to all 4 locale files</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/zh-TW.json, frontend/src/locales/th.json, frontend/src/locales/vi.json</files>
  <read_first>frontend/src/locales/en.json, frontend/src/locales/zh-TW.json, frontend/src/locales/th.json, frontend/src/locales/vi.json</read_first>
  <action>
Add an `"auth"` section to each locale file (per D-12). This section contains form labels, button text, link text, and error messages for login and registration.

The existing `"login"` section stays as-is. The new `"auth"` section covers the form-based auth UI.

Add the following keys to each locale file. Place the `"auth"` section after the existing `"login"` section.

**en.json** — add after `"login"` block:
```json
"auth": {
  "emailLabel": "Email",
  "passwordLabel": "Password",
  "confirmPasswordLabel": "Confirm Password",
  "loginButton": "Log In",
  "registerButton": "Create Account",
  "registerLink": "Don't have an account? Register",
  "loginLink": "Already have an account? Log in",
  "signInWithMicrosoft": "Sign in with Microsoft",
  "orDivider": "or",
  "errorInvalidCredentials": "Invalid email or password",
  "errorEmailTaken": "Email already registered",
  "errorEmailTakenSso": "Email already in use. Log in via SSO to link your account.",
  "errorPasswordMismatch": "Passwords do not match",
  "errorEmailRequired": "Email is required",
  "errorPasswordRequired": "Password is required",
  "errorPasswordTooLong": "Password must be 72 characters or fewer",
  "errorInvalidInput": "Please check your input and try again",
  "errorAccountDisabled": "Account is disabled",
  "errorRateLimited": "Too many attempts. Please wait and try again.",
  "errorNetworkError": "Network error. Please check your connection.",
  "errorUnexpected": "Something went wrong. Please try again."
}
```

**zh-TW.json** — add after `"login"` block:
```json
"auth": {
  "emailLabel": "電子郵件",
  "passwordLabel": "密碼",
  "confirmPasswordLabel": "確認密碼",
  "loginButton": "登入",
  "registerButton": "建立帳號",
  "registerLink": "還沒有帳號？註冊",
  "loginLink": "已有帳號？登入",
  "signInWithMicrosoft": "使用 Microsoft 帳號登入",
  "orDivider": "或",
  "errorInvalidCredentials": "電子郵件或密碼不正確",
  "errorEmailTaken": "此電子郵件已被註冊",
  "errorEmailTakenSso": "此電子郵件已被使用，請透過 SSO 登入以連結帳號。",
  "errorPasswordMismatch": "密碼不一致",
  "errorEmailRequired": "請輸入電子郵件",
  "errorPasswordRequired": "請輸入密碼",
  "errorPasswordTooLong": "密碼不得超過 72 個字元",
  "errorInvalidInput": "請檢查輸入內容後重試",
  "errorAccountDisabled": "帳號已停用",
  "errorRateLimited": "嘗試次數過多，請稍後再試。",
  "errorNetworkError": "網路錯誤，請檢查連線。",
  "errorUnexpected": "發生錯誤，請重試。"
}
```

**th.json** — add after `"login"` block:
```json
"auth": {
  "emailLabel": "อีเมล",
  "passwordLabel": "รหัสผ่าน",
  "confirmPasswordLabel": "ยืนยันรหัสผ่าน",
  "loginButton": "เข้าสู่ระบบ",
  "registerButton": "สร้างบัญชี",
  "registerLink": "ยังไม่มีบัญชี? ลงทะเบียน",
  "loginLink": "มีบัญชีแล้ว? เข้าสู่ระบบ",
  "signInWithMicrosoft": "เข้าสู่ระบบด้วย Microsoft",
  "orDivider": "หรือ",
  "errorInvalidCredentials": "อีเมลหรือรหัสผ่านไม่ถูกต้อง",
  "errorEmailTaken": "อีเมลนี้ถูกลงทะเบียนแล้ว",
  "errorEmailTakenSso": "อีเมลนี้ถูกใช้แล้ว กรุณาเข้าสู่ระบบผ่าน SSO เพื่อเชื่อมต่อบัญชี",
  "errorPasswordMismatch": "รหัสผ่านไม่ตรงกัน",
  "errorEmailRequired": "กรุณากรอกอีเมล",
  "errorPasswordRequired": "กรุณากรอกรหัสผ่าน",
  "errorPasswordTooLong": "รหัสผ่านต้องไม่เกิน 72 ตัวอักษร",
  "errorInvalidInput": "กรุณาตรวจสอบข้อมูลแล้วลองอีกครั้ง",
  "errorAccountDisabled": "บัญชีถูกปิดใช้งาน",
  "errorRateLimited": "ลองหลายครั้งเกินไป กรุณารอสักครู่แล้วลองอีกครั้ง",
  "errorNetworkError": "เกิดข้อผิดพลาดของเครือข่าย กรุณาตรวจสอบการเชื่อมต่อ",
  "errorUnexpected": "เกิดข้อผิดพลาด กรุณาลองอีกครั้ง"
}
```

**vi.json** — add after `"login"` block:
```json
"auth": {
  "emailLabel": "Email",
  "passwordLabel": "Mật khẩu",
  "confirmPasswordLabel": "Xác nhận mật khẩu",
  "loginButton": "Đăng nhập",
  "registerButton": "Tạo tài khoản",
  "registerLink": "Chưa có tài khoản? Đăng ký",
  "loginLink": "Đã có tài khoản? Đăng nhập",
  "signInWithMicrosoft": "Đăng nhập bằng Microsoft",
  "orDivider": "hoặc",
  "errorInvalidCredentials": "Email hoặc mật khẩu không đúng",
  "errorEmailTaken": "Email đã được đăng ký",
  "errorEmailTakenSso": "Email đã được sử dụng. Vui lòng đăng nhập qua SSO để liên kết tài khoản.",
  "errorPasswordMismatch": "Mật khẩu không khớp",
  "errorEmailRequired": "Vui lòng nhập email",
  "errorPasswordRequired": "Vui lòng nhập mật khẩu",
  "errorPasswordTooLong": "Mật khẩu không được quá 72 ký tự",
  "errorInvalidInput": "Vui lòng kiểm tra lại thông tin và thử lại",
  "errorAccountDisabled": "Tài khoản đã bị vô hiệu hóa",
  "errorRateLimited": "Quá nhiều lần thử. Vui lòng đợi và thử lại.",
  "errorNetworkError": "Lỗi mạng. Vui lòng kiểm tra kết nối.",
  "errorUnexpected": "Đã xảy ra lỗi. Vui lòng thử lại."
}
```

The error code to i18n key mapping used by the frontend (for reference — implemented in Plan 02):
- `auth.invalid_credentials` -> `t('auth.errorInvalidCredentials')`
- `auth.email_taken` -> `t('auth.errorEmailTaken')` (check message for SSO variant)
- `auth.password_too_long` -> `t('auth.errorPasswordTooLong')`
- `auth.invalid_input` -> `t('auth.errorInvalidInput')`
- `auth.account_disabled` -> `t('auth.errorAccountDisabled')`
- HTTP 429 -> `t('auth.errorRateLimited')`
- Network error (fetch throws) -> `t('auth.errorNetworkError')`
- Any other error -> `t('auth.errorUnexpected')`
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard/frontend && grep -q "auth.emailLabel" src/locales/en.json && grep -q "auth.emailLabel" src/locales/zh-TW.json && grep -q "auth.emailLabel" src/locales/th.json && grep -q "auth.emailLabel" src/locales/vi.json && grep -q "errorInvalidCredentials" src/locales/en.json && grep -q "errorRateLimited" src/locales/en.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 locale files contain an `"auth"` section with at least these keys: emailLabel, passwordLabel, confirmPasswordLabel, loginButton, registerButton, registerLink, loginLink, signInWithMicrosoft, orDivider
    - All 4 locale files contain error keys: errorInvalidCredentials, errorEmailTaken, errorEmailTakenSso, errorPasswordMismatch, errorEmailRequired, errorPasswordRequired, errorPasswordTooLong, errorInvalidInput, errorAccountDisabled, errorRateLimited, errorNetworkError, errorUnexpected
    - en.json `"auth.emailLabel"` value is `"Email"`
    - zh-TW.json `"auth.emailLabel"` value is `"電子郵件"`
    - th.json `"auth.emailLabel"` value is `"อีเมล"`
    - vi.json `"auth.emailLabel"` value is `"Email"`
    - The existing `"login"` section in each file is unchanged
    - All 4 files are valid JSON (no syntax errors)
  </acceptance_criteria>
  <done>All 4 locale files have complete auth form and error i18n keys with correct translations</done>
</task>

</tasks>

<verification>
- `grep -c "auth\." frontend/src/locales/en.json` returns at least 20 (auth section keys)
- `node -e "JSON.parse(require('fs').readFileSync('frontend/src/locales/en.json'))"` exits 0 (valid JSON for all 4 files)
- `grep "skipRefresh" frontend/src/lib/api.ts` finds the 401 exemption logic
</verification>

<success_criteria>
- apiFetch skips token refresh for /auth/login/local and /auth/register paths
- All 4 locale files have matching auth key sets (same keys, language-appropriate values)
- No existing functionality is broken (existing login keys preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-auth-ui/03-01-SUMMARY.md`
</output>
