---
phase: 02-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/apierr/apierr.go
  - internal/apierr/apierr_test.go
  - cmd/server/main.go
  - internal/auth/middleware.go
  - internal/rbac/middleware.go
autonomous: true
requirements: [ERR-01, ERR-03]

must_haves:
  truths:
    - "apierr.Write() returns Content-Type application/json with {code, message} JSON body"
    - "apierr.Write() logs via slog.Error with request_id, user_id, method, path, error_code fields"
    - "RequestID middleware is first in the chain, before Logger and Recoverer"
    - "Auth and RBAC middleware errors return structured JSON instead of plain text"
  artifacts:
    - path: "internal/apierr/apierr.go"
      provides: "Shared error writer with slog logging"
      exports: ["Write"]
    - path: "internal/apierr/apierr_test.go"
      provides: "Unit tests for Write function — JSON shape and slog fields"
      min_lines: 40
  key_links:
    - from: "internal/apierr/apierr.go"
      to: "chi middleware"
      via: "chiMiddleware.GetReqID(r.Context())"
      pattern: "GetReqID"
    - from: "internal/auth/middleware.go"
      to: "internal/apierr"
      via: "apierr.Write() calls"
      pattern: "apierr\\.Write"
    - from: "internal/rbac/middleware.go"
      to: "internal/apierr"
      via: "apierr.Write() calls"
      pattern: "apierr\\.Write"
---

<objective>
Create the shared `internal/apierr` package with a `Write()` function that combines JSON error response and slog logging, add `chiMiddleware.RequestID` to the middleware chain, and convert auth/rbac middleware to use the new package.

Purpose: Establishes the error infrastructure that all subsequent plans depend on, and converts the middleware layer (which is in every request path) first.
Output: `internal/apierr/apierr.go`, `internal/apierr/apierr_test.go`, updated `cmd/server/main.go`, updated middleware files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-error-handling/02-CONTEXT.md
@.planning/phases/02-error-handling/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From internal/auth/errors.go (source of truth to promote):
```go
type apiError struct {
    Code    string `json:"code"`
    Message string `json:"message"`
}

func writeError(w http.ResponseWriter, status int, code, message string) {
    w.Header().Set("Content-Type", "application/json")
    w.WriteHeader(status)
    json.NewEncoder(w).Encode(apiError{Code: code, Message: message})
}
```

From internal/auth/jwt.go (Claims struct for userID extraction):
```go
type Claims struct {
    UserID    string `json:"user_id"`
    Email     string `json:"email"`
    TokenType string `json:"token_type"`
    jwt.RegisteredClaims
}
```

From internal/auth/middleware.go:
```go
func GetClaims(ctx context.Context) *Claims
func SetClaims(ctx context.Context, claims *Claims) context.Context
```

From cmd/server/main.go lines 112-113 (current middleware chain):
```go
r.Use(chiMiddleware.Logger)
r.Use(chiMiddleware.Recoverer)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create internal/apierr package with Write() and tests</name>
  <files>internal/apierr/apierr.go, internal/apierr/apierr_test.go</files>
  <read_first>
    - internal/auth/errors.go (source of truth for apiError struct and writeError)
    - internal/auth/jwt.go (Claims struct — need UserID field name)
    - internal/auth/middleware.go (GetClaims function — understand context extraction pattern)
  </read_first>
  <behavior>
    - Test 1: Write() sets Content-Type to "application/json"
    - Test 2: Write() sets the correct HTTP status code
    - Test 3: Write() response body is valid JSON with exactly {code, message} fields
    - Test 4: Write() calls slog.Error with fields: request_id, user_id, method, path, error_code, error
    - Test 5: Write() with empty userID logs user_id as ""
    - Test 6: Write() with nil err logs error as nil (no panic)
  </behavior>
  <action>
    Create `internal/apierr/apierr.go` per D-01, D-02, D-07.

    The package must NOT import `internal/auth` — avoids circular import (Pitfall 4 from RESEARCH.md). userID is passed as a string parameter.

    Exact function signature (per RESEARCH.md Pitfall 4 resolution, Option A):
    ```go
    func Write(w http.ResponseWriter, r *http.Request, status int, code, message, userID string, err error)
    ```

    Implementation:
    1. Define unexported `apiError` struct with `Code string json:"code"` and `Message string json:"message"` (per D-02)
    2. `Write()` first calls `slog.Error("api error", "request_id", chiMiddleware.GetReqID(r.Context()), "user_id", userID, "method", r.Method, "path", r.URL.Path, "error_code", code, "error", err)` (per D-06)
    3. Then sets `Content-Type: application/json` header
    4. Then calls `w.WriteHeader(status)`
    5. Then encodes `apiError{Code: code, Message: message}` as JSON (per D-02)

    Imports: `encoding/json`, `log/slog`, `net/http`, `chiMiddleware "github.com/go-chi/chi/v5/middleware"`

    For tests in `apierr_test.go`:
    - Use `httptest.NewRecorder()` for response assertions
    - Use `httptest.NewRequest()` for request context
    - To verify slog output, replace the default logger with `slog.New(slog.NewTextHandler(&buf, nil))` via `slog.SetDefault()`, then restore after test. Check `buf.String()` contains expected fields.
    - To set request ID in test context, use `chiMiddleware.RequestIDHeader` or set the chi request ID context directly via `middleware.WithRequestID(ctx, "test-req-id")` — if that's unavailable, insert `chiMiddleware.RequestID` handler in a test handler chain.
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard && go test ./internal/apierr/... -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/apierr/apierr.go` exists and contains `func Write(w http.ResponseWriter, r *http.Request, status int, code, message, userID string, err error)`
    - File `internal/apierr/apierr.go` contains `slog.Error("api error"`
    - File `internal/apierr/apierr.go` contains `"request_id", chiMiddleware.GetReqID(r.Context())`
    - File `internal/apierr/apierr.go` contains `json.NewEncoder(w).Encode`
    - File `internal/apierr/apierr.go` does NOT contain `internal/auth` in imports (no circular import)
    - File `internal/apierr/apierr_test.go` exists with at least 4 test functions
    - `go test ./internal/apierr/... -v` exits 0
  </acceptance_criteria>
  <done>apierr.Write() produces {code, message} JSON responses and logs all required fields via slog. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add RequestID middleware and convert auth/rbac middleware</name>
  <files>cmd/server/main.go, internal/auth/middleware.go, internal/rbac/middleware.go</files>
  <read_first>
    - cmd/server/main.go (full file — find exact middleware chain location and all http.Error calls in dev routes)
    - internal/auth/middleware.go (full file — find all 5 http.Error calls to convert)
    - internal/rbac/middleware.go (full file — find all 3 http.Error calls to convert)
    - internal/apierr/apierr.go (just created — verify Write signature)
  </read_first>
  <action>
    **Step 1: cmd/server/main.go — Add RequestID middleware (per D-05)**

    Find the lines:
    ```go
    r.Use(chiMiddleware.Logger)
    r.Use(chiMiddleware.Recoverer)
    ```
    Insert `r.Use(chiMiddleware.RequestID)` BEFORE `chiMiddleware.Logger` so request IDs are available to all subsequent middleware. The order MUST be: RequestID, Logger, Recoverer, cors (per Pitfall 2 from RESEARCH.md).

    **Step 2: internal/auth/middleware.go — Convert 5 http.Error calls**

    Replace every `http.Error(w, "unauthorized", http.StatusUnauthorized)` and `http.Error(w, "API keys are read-only", http.StatusForbidden)` with `apierr.Write()` calls.

    For each call site:
    - Extract userID: since auth middleware fires before claims are set, `userID` will be `""` for all error paths in this file
    - Pass `nil` as the `err` parameter for client auth errors (no underlying server error)

    Specific replacements (5 calls):
    1. Line ~44: `http.Error(w, "unauthorized", http.StatusUnauthorized)` -> `apierr.Write(w, r, http.StatusUnauthorized, "auth.unauthorized", "Unauthorized", "", nil)`
    2. Line ~53: `http.Error(w, "unauthorized", http.StatusUnauthorized)` -> `apierr.Write(w, r, http.StatusUnauthorized, "auth.unauthorized", "Unauthorized", "", nil)`
    3. Line ~58: `http.Error(w, "unauthorized", http.StatusUnauthorized)` -> `apierr.Write(w, r, http.StatusUnauthorized, "auth.unauthorized", "Unauthorized", "", nil)`
    4. Line ~68: `http.Error(w, "API keys are read-only", http.StatusForbidden)` -> `apierr.Write(w, r, http.StatusForbidden, "auth.read_only_key", "API keys are read-only", "", nil)`
    5. Line ~79: `http.Error(w, "unauthorized", http.StatusUnauthorized)` -> `apierr.Write(w, r, http.StatusUnauthorized, "auth.unauthorized", "Unauthorized", "", nil)`

    Add import: `"github.com/industry-dashboard/server/internal/apierr"` (use the actual module path from go.mod)

    **Step 3: internal/rbac/middleware.go — Convert 3 http.Error calls**

    Read the file to find all 3 `http.Error` calls. Replace each with `apierr.Write()`.

    For RBAC middleware, claims ARE available (it runs after auth middleware), so extract userID:
    ```go
    userID := ""
    if claims := auth.GetClaims(r.Context()); claims != nil {
        userID = claims.UserID
    }
    ```

    Use error codes with `rbac.` prefix per D-03:
    - `rbac.forbidden` for permission denied (403)
    - `rbac.unauthorized` for missing claims (401)

    Add import: `"github.com/industry-dashboard/server/internal/apierr"` (use the actual module path from go.mod)
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard && go build ./... && go test ./internal/auth/... ./internal/rbac/... -v</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/server/main.go` contains `r.Use(chiMiddleware.RequestID)` BEFORE `r.Use(chiMiddleware.Logger)`
    - `grep -c "http\.Error" internal/auth/middleware.go` returns 0
    - `grep -c "http\.Error" internal/rbac/middleware.go` returns 0
    - `grep -c "apierr.Write" internal/auth/middleware.go` returns 5
    - `grep -c "apierr.Write" internal/rbac/middleware.go` returns 3
    - `go build ./...` exits 0 (no circular imports)
    - `go test ./internal/auth/... ./internal/rbac/... -v` exits 0
  </acceptance_criteria>
  <done>RequestID middleware is first in the chain. Auth middleware (5 calls) and RBAC middleware (3 calls) return structured JSON errors with slog logging. Zero http.Error calls remain in middleware files.</done>
</task>

</tasks>

<verification>
- `go test ./internal/apierr/... -v` — apierr package tests pass
- `go test ./internal/auth/... ./internal/rbac/... -v` — middleware tests pass
- `go build ./...` — entire project compiles (no circular imports)
- `grep -rn "http\.Error" internal/auth/middleware.go internal/rbac/middleware.go` — returns 0 results
</verification>

<success_criteria>
- `internal/apierr` package exists with Write() function that produces {code, message} JSON and logs via slog
- RequestID middleware is registered before Logger in cmd/server/main.go
- Auth and RBAC middleware use apierr.Write() exclusively — no http.Error() calls remain
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-error-handling/02-01-SUMMARY.md`
</output>
