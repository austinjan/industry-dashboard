---
phase: 02-error-handling
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/auth/handler.go
  - internal/auth/local.go
  - internal/auth/errors.go
  - internal/site/handler.go
  - internal/alert/handler.go
  - internal/worker_config/handler.go
  - internal/worker_api/handler.go
  - internal/dashboard/handler.go
autonomous: true
requirements: [ERR-01, ERR-03]

must_haves:
  truths:
    - "Auth handler OIDC errors return structured JSON with auth.* error codes"
    - "Auth local.go uses apierr.Write instead of package-local writeError, no double-logging from log.Printf"
    - "internal/auth/errors.go is deleted — no package-local error writer remains"
    - "All 5 large domain handlers return structured JSON errors with package-prefixed codes"
  artifacts:
    - path: "internal/site/handler.go"
      provides: "43 http.Error calls converted to apierr.Write with site.* codes"
      contains: "apierr.Write"
    - path: "internal/alert/handler.go"
      provides: "20 http.Error calls converted to apierr.Write with alert.* codes"
      contains: "apierr.Write"
    - path: "internal/dashboard/handler.go"
      provides: "15 http.Error calls converted to apierr.Write with dashboard.* codes"
      contains: "apierr.Write"
  key_links:
    - from: "internal/site/handler.go"
      to: "internal/apierr"
      via: "apierr.Write() calls"
      pattern: "apierr\\.Write"
    - from: "internal/auth/local.go"
      to: "internal/apierr"
      via: "apierr.Write() replacing writeError()"
      pattern: "apierr\\.Write"
---

<objective>
Migrate the auth package (handler.go, local.go, delete errors.go) and the 5 largest domain handler files (site, alert, worker_config, worker_api, dashboard) from http.Error() to apierr.Write(). This covers 124 of the 162 total http.Error calls.

Purpose: Converts the bulk of the codebase to structured errors. After this plan, only 18 calls remain in small handlers and dev routes.
Output: 8 files modified, 1 file deleted, 124 http.Error calls eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-error-handling/02-CONTEXT.md
@.planning/phases/02-error-handling/02-RESEARCH.md
@.planning/phases/02-error-handling/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the apierr.Write signature -->
From internal/apierr/apierr.go:
```go
// Write writes a structured JSON error response and logs it via slog.
// userID should be claims.UserID if authenticated, "" if not.
// err is the underlying error for logging; pass nil for client-side errors.
func Write(w http.ResponseWriter, r *http.Request, status int, code, message, userID string, err error)
```

From internal/auth/middleware.go (for extracting userID in handlers):
```go
func GetClaims(ctx context.Context) *Claims
// Claims.UserID is the field to pass to apierr.Write
```

Error code prefix conventions (per D-03, D-08, D-09):
- auth.* — auth.invalid_input, auth.invalid_credentials, auth.email_taken, auth.unauthorized
- site.* — site.not_found, site.invalid_input, site.code_exists
- alert.* — alert.not_found, alert.invalid_input, alert.invalid_request
- worker.* — worker.not_found, worker.invalid_input
- dashboard.* — dashboard.not_found, dashboard.invalid_input
- internal — for unexpected server errors (D-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert auth package — handler.go, local.go, delete errors.go</name>
  <files>internal/auth/handler.go, internal/auth/local.go, internal/auth/errors.go</files>
  <read_first>
    - internal/auth/handler.go (full file — find all 11 http.Error calls)
    - internal/auth/local.go (full file — find all writeError calls and log.Printf calls before them)
    - internal/auth/errors.go (confirm it only has apiError struct and writeError — to be deleted)
    - internal/apierr/apierr.go (verify Write signature from Plan 01)
  </read_first>
  <action>
    **Step 1: internal/auth/handler.go — Convert 11 http.Error calls**

    For each `http.Error(w, ..., status)` call, replace with `apierr.Write(w, r, status, code, message, userID, err)`.

    UserID extraction pattern for handler methods (claims may or may not be available depending on the route):
    ```go
    userID := ""
    if claims := GetClaims(r.Context()); claims != nil {
        userID = claims.UserID
    }
    ```
    Note: `GetClaims` is in the same package so no import needed.

    Error code mapping for OIDC handler errors (per D-03):
    - Bad request / invalid input -> `"auth.invalid_input"`, 400
    - Internal/server errors -> `"internal"`, 500 (per D-04)
    - Unauthorized -> `"auth.unauthorized"`, 401

    Add import: `"github.com/industry-dashboard/server/internal/apierr"` (use actual module path from go.mod)

    **Step 2: internal/auth/local.go — Replace writeError with apierr.Write**

    Replace every `writeError(w, status, code, message)` call with `apierr.Write(w, r, status, code, message, userID, err)`.

    CRITICAL (Pitfall 6 from RESEARCH.md): Remove standalone `log.Printf()` calls that precede a `writeError()` call. The `apierr.Write()` function handles logging now — keeping `log.Printf` would cause double-logging.

    For each replacement:
    1. Find the `log.Printf(...)` line before `writeError(...)`
    2. Delete the `log.Printf` line
    3. Replace `writeError(w, status, code, message)` with `apierr.Write(w, r, status, code, message, userID, err)` where `err` is the underlying error that was being logged by log.Printf (or nil if it was a client error)

    UserID extraction: same pattern as handler.go. In local.go, most error paths fire before login succeeds so userID will be "".

    Update imports: add `apierr` import, remove `"log"` import if no longer used.

    **Step 3: Delete internal/auth/errors.go**

    Per D-11 (clean cut, no shim): delete the file entirely. The `apiError` struct and `writeError` function are now in `internal/apierr`. Verify `go build ./internal/auth/...` compiles after deletion — no other file in auth package should reference `writeError` or `apiError` after Steps 1-2.
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard && go build ./internal/auth/... && go test ./internal/auth/... -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/auth/errors.go` does NOT exist (deleted)
    - `grep -c "http\.Error" internal/auth/handler.go` returns 0
    - `grep -c "writeError" internal/auth/local.go` returns 0
    - `grep -c "apierr.Write" internal/auth/handler.go` is at least 10
    - `grep -c "apierr.Write" internal/auth/local.go` is at least 5
    - `grep -c "log\.Printf" internal/auth/local.go` returns 0 (no double-logging)
    - `go build ./internal/auth/...` exits 0
  </acceptance_criteria>
  <done>Auth package uses apierr.Write exclusively. errors.go deleted. No standalone log.Printf before error writes. All auth tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Convert 5 large domain handlers (site, alert, worker_config, worker_api, dashboard)</name>
  <files>internal/site/handler.go, internal/alert/handler.go, internal/worker_config/handler.go, internal/worker_api/handler.go, internal/dashboard/handler.go</files>
  <read_first>
    - internal/site/handler.go (full file — 43 http.Error calls, largest file)
    - internal/alert/handler.go (full file — 20 http.Error calls)
    - internal/worker_config/handler.go (full file — 19 http.Error calls)
    - internal/worker_api/handler.go (full file — 16 http.Error calls)
    - internal/dashboard/handler.go (full file — 15 http.Error calls)
    - internal/apierr/apierr.go (verify Write signature)
  </read_first>
  <action>
    For each of the 5 handler files, systematically replace every `http.Error(w, msg, status)` call with `apierr.Write(w, r, status, code, message, userID, err)`.

    **Common pattern for all files:**

    1. Add import: `"github.com/industry-dashboard/server/internal/apierr"` and `"github.com/industry-dashboard/server/internal/auth"` (for GetClaims, if not already imported)

    2. At the top of each handler method, extract userID:
    ```go
    userID := ""
    if claims := auth.GetClaims(r.Context()); claims != nil {
        userID = claims.UserID
    }
    ```
    If multiple error returns in one method, extract userID once at the top. If userID extraction already exists (some handlers may already use claims), reuse it.

    3. Error code mapping per package prefix (per D-03, D-08, D-09):

    **internal/site/handler.go (43 calls) — prefix `site.`:**
    - `http.Error(w, "...", http.StatusBadRequest)` -> `apierr.Write(w, r, http.StatusBadRequest, "site.invalid_input", "...", userID, nil)`
    - `http.Error(w, "...", http.StatusNotFound)` -> `apierr.Write(w, r, http.StatusNotFound, "site.not_found", "...", userID, nil)`
    - `http.Error(w, "...", http.StatusInternalServerError)` -> `apierr.Write(w, r, http.StatusInternalServerError, "internal", "Internal error", userID, err)` (per D-04, pass the actual err)
    - `http.Error(w, "...", http.StatusConflict)` -> `apierr.Write(w, r, http.StatusConflict, "site.code_exists", "...", userID, nil)`
    - Preserve the original human-readable message as the `message` parameter

    **internal/alert/handler.go (20 calls) — prefix `alert.`:**
    - Bad request -> `"alert.invalid_input"` or `"alert.invalid_request"` (use `invalid_input` for body parsing, `invalid_request` for query param issues)
    - Not found -> `"alert.not_found"`
    - Internal errors -> `"internal"` (per D-04)

    **internal/worker_config/handler.go (19 calls) — prefix `worker.`:**
    - Bad request -> `"worker.invalid_input"`
    - Not found -> `"worker.not_found"`
    - Internal errors -> `"internal"`

    **internal/worker_api/handler.go (16 calls) — prefix `worker.`:**
    - Same pattern as worker_config — `"worker.invalid_input"`, `"worker.not_found"`, `"internal"`

    **internal/dashboard/handler.go (15 calls) — prefix `dashboard.`:**
    - Bad request -> `"dashboard.invalid_input"`
    - Not found -> `"dashboard.not_found"`
    - Forbidden/access denied -> `"dashboard.forbidden"`
    - Internal errors -> `"internal"`

    4. For every `http.StatusInternalServerError` replacement, pass the actual `err` variable as the last parameter (not nil). For 4xx client errors, pass `nil`.

    5. If any file uses standalone `log.Printf` or `log.Println` before an `http.Error` call (similar to auth/local.go pattern), remove the log call to avoid double-logging.
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard && go build ./internal/site/... ./internal/alert/... ./internal/worker_config/... ./internal/worker_api/... ./internal/dashboard/... && go test ./internal/site/... ./internal/alert/... ./internal/worker_config/... ./internal/worker_api/... ./internal/dashboard/... -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "http\.Error" internal/site/handler.go` returns 0
    - `grep -c "http\.Error" internal/alert/handler.go` returns 0
    - `grep -c "http\.Error" internal/worker_config/handler.go` returns 0
    - `grep -c "http\.Error" internal/worker_api/handler.go` returns 0
    - `grep -c "http\.Error" internal/dashboard/handler.go` returns 0
    - `grep -c "apierr.Write" internal/site/handler.go` is at least 40
    - `grep -c "apierr.Write" internal/alert/handler.go` is at least 18
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>113 http.Error calls across 5 domain handler files converted to apierr.Write with proper package-prefixed error codes and slog logging. All tests pass.</done>
</task>

</tasks>

<verification>
- `go build ./...` — entire project compiles
- `go test ./internal/auth/... ./internal/site/... ./internal/alert/... ./internal/worker_config/... ./internal/worker_api/... ./internal/dashboard/... -v` — all tests pass
- `grep -rn "http\.Error" internal/auth/ internal/site/ internal/alert/ internal/worker_config/ internal/worker_api/ internal/dashboard/` — returns 0 results
</verification>

<success_criteria>
- Auth package fully migrated: handler.go uses apierr.Write, local.go uses apierr.Write (no writeError, no log.Printf double-logging), errors.go deleted
- 5 domain handler files (site, alert, worker_config, worker_api, dashboard) use apierr.Write exclusively
- 124 total http.Error calls eliminated
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-error-handling/02-02-SUMMARY.md`
</output>
