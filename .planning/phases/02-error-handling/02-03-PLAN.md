---
phase: 02-error-handling
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/rbac/handler.go
  - internal/llmauth/handler.go
  - internal/datapoint/handler.go
  - internal/user/preference.go
  - internal/user/handler.go
  - internal/audit/handler.go
  - cmd/server/main.go
autonomous: true
requirements: [ERR-01, ERR-03]

must_haves:
  truths:
    - "Zero http.Error calls remain anywhere in internal/ or cmd/server/main.go"
    - "Every remaining handler file uses apierr.Write with appropriate package-prefixed error codes"
    - "Dev routes in main.go also use structured errors per D-11 (no exceptions)"
  artifacts:
    - path: "internal/rbac/handler.go"
      provides: "10 http.Error calls converted with rbac.* codes"
      contains: "apierr.Write"
    - path: "internal/llmauth/handler.go"
      provides: "6 http.Error calls converted with llm.* codes"
      contains: "apierr.Write"
  key_links:
    - from: "internal/rbac/handler.go"
      to: "internal/apierr"
      via: "apierr.Write() calls"
      pattern: "apierr\\.Write"
    - from: "cmd/server/main.go"
      to: "internal/apierr"
      via: "apierr.Write() in dev routes"
      pattern: "apierr\\.Write"
---

<objective>
Convert the remaining 6 handler files (rbac, llmauth, datapoint, user/preference, user/handler, audit) and the 4 dev-route http.Error calls in cmd/server/main.go. Run final verification sweep to confirm zero http.Error calls remain.

Purpose: Completes the migration — after this plan, ERR-01 is fully satisfied (100% structured JSON errors) and ERR-03 is fully satisfied (all errors logged with request context).
Output: 7 files modified, 38 http.Error calls eliminated, zero remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-error-handling/02-CONTEXT.md
@.planning/phases/02-error-handling/02-RESEARCH.md
@.planning/phases/02-error-handling/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the apierr.Write signature -->
From internal/apierr/apierr.go:
```go
func Write(w http.ResponseWriter, r *http.Request, status int, code, message, userID string, err error)
```

Error code prefix conventions (per D-03, D-08, D-09):
- rbac.* — rbac.not_found, rbac.invalid_input, rbac.forbidden
- llm.* — llm.invalid_input, llm.not_found, llm.unauthorized
- datapoint.* — datapoint.invalid_input
- user.* — user.invalid_input
- audit.* — audit.invalid_input
- internal — for unexpected server errors (D-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert 6 remaining handler files</name>
  <files>internal/rbac/handler.go, internal/llmauth/handler.go, internal/datapoint/handler.go, internal/user/preference.go, internal/user/handler.go, internal/audit/handler.go</files>
  <read_first>
    - internal/rbac/handler.go (full file — 10 http.Error calls)
    - internal/llmauth/handler.go (full file — 6 http.Error calls)
    - internal/datapoint/handler.go (full file — 4 http.Error calls)
    - internal/user/preference.go (full file — 4 http.Error calls)
    - internal/user/handler.go (full file — 1 http.Error call)
    - internal/audit/handler.go (full file — 1 http.Error call)
    - internal/apierr/apierr.go (verify Write signature)
  </read_first>
  <action>
    Apply the same conversion pattern as Plan 02 Task 2 to all 6 remaining handler files.

    For each file:
    1. Add import `apierr` (and `auth` if not already imported for GetClaims)
    2. In each handler method, extract userID once at the top:
       ```go
       userID := ""
       if claims := auth.GetClaims(r.Context()); claims != nil {
           userID = claims.UserID
       }
       ```
    3. Replace every `http.Error(w, msg, status)` with `apierr.Write(w, r, status, code, message, userID, err)`

    **Per-file error code mappings:**

    **internal/rbac/handler.go (10 calls) — prefix `rbac.`:**
    - Bad request -> `"rbac.invalid_input"`
    - Not found -> `"rbac.not_found"`
    - Internal -> `"internal"` (per D-04, pass actual err)

    **internal/llmauth/handler.go (6 calls) — prefix `llm.`:**
    - Bad request -> `"llm.invalid_input"`
    - Not found -> `"llm.not_found"`
    - Unauthorized -> `"llm.unauthorized"`
    - Internal -> `"internal"`

    **internal/datapoint/handler.go (4 calls) — prefix `datapoint.`:**
    - Bad request -> `"datapoint.invalid_input"`
    - Internal -> `"internal"`

    **internal/user/preference.go (4 calls) — prefix `user.`:**
    - Bad request -> `"user.invalid_input"`
    - Internal -> `"internal"`

    **internal/user/handler.go (1 call) — prefix `user.`:**
    - Likely internal error -> `"internal"` or `"user.invalid_input"` depending on context

    **internal/audit/handler.go (1 call) — prefix `audit.`:**
    - Likely bad request -> `"audit.invalid_input"` or internal -> `"internal"`

    For all 500 errors, pass the actual `err` variable. For 4xx errors, pass `nil`.

    Remove any standalone `log.Printf`/`log.Println` that precede `http.Error` calls to avoid double-logging.
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard && go build ./internal/rbac/... ./internal/llmauth/... ./internal/datapoint/... ./internal/user/... ./internal/audit/... && go test ./internal/rbac/... ./internal/llmauth/... ./internal/datapoint/... ./internal/user/... ./internal/audit/... -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "http\.Error" internal/rbac/handler.go` returns 0
    - `grep -c "http\.Error" internal/llmauth/handler.go` returns 0
    - `grep -c "http\.Error" internal/datapoint/handler.go` returns 0
    - `grep -c "http\.Error" internal/user/preference.go` returns 0
    - `grep -c "http\.Error" internal/user/handler.go` returns 0
    - `grep -c "http\.Error" internal/audit/handler.go` returns 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>All 6 remaining handler files use apierr.Write exclusively. 26 http.Error calls eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: Convert main.go dev routes and run full verification sweep</name>
  <files>cmd/server/main.go</files>
  <read_first>
    - cmd/server/main.go (full file — find the 4 http.Error calls in dev routes, verify RequestID middleware was added by Plan 01)
    - internal/apierr/apierr.go (verify Write signature)
  </read_first>
  <action>
    **Step 1: Convert 4 http.Error calls in cmd/server/main.go dev routes**

    Per D-11 (clean cut) and Pitfall 5 from RESEARCH.md: dev routes must also use structured errors.

    Find the dev routes (gated by `DEV_MODE`/`cfg.DevMode`) that contain `http.Error` calls. These are inline closures for `/dev/seed`, `/dev/seed-data`, `/dev/login` etc.

    For each:
    - Replace `http.Error(w, msg, status)` with `apierr.Write(w, r, status, code, message, "", err)`
    - userID is `""` for dev routes (they bypass auth)
    - Error codes: use `"internal"` for server errors in dev routes (they don't belong to a domain package)

    Add import `apierr` if not already present (it may already be imported if Plan 01 added it for RequestID).

    **Step 2: Full verification sweep**

    Run these commands to confirm complete migration:
    ```bash
    # Must return 0 lines — no http.Error in any Go source
    grep -rn "http\.Error" internal/ cmd/server/main.go

    # Must return many lines — apierr.Write is used everywhere
    grep -c "apierr.Write" internal/*/handler.go internal/*/middleware.go internal/auth/local.go cmd/server/main.go

    # Full test suite
    make test

    # Compilation
    go build ./...
    ```
  </action>
  <verify>
    <automated>cd /Users/macmini-au/code/industry-dashboard && grep -rn "http\.Error" internal/ cmd/server/main.go | grep -v "_test.go" | wc -l | tr -d ' ' && echo "--- must be 0 above ---" && make test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "http\.Error" internal/ cmd/server/main.go | grep -v _test.go` returns 0 lines
    - `grep -c "apierr.Write" cmd/server/main.go` returns at least 4
    - `make test` exits 0 (full test suite passes)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Zero http.Error calls remain in production code (internal/ and cmd/server/main.go). Full test suite passes. ERR-01 and ERR-03 are fully satisfied.</done>
</task>

</tasks>

<verification>
- `grep -rn "http\.Error" internal/ cmd/server/main.go | grep -v _test.go` — returns 0 lines (phase gate)
- `make test` — full test suite passes
- `go build ./...` — project compiles cleanly
- `grep -rn "apierr\.Write" internal/ cmd/server/main.go | wc -l` — returns 162+ (all calls converted)
</verification>

<success_criteria>
- Zero http.Error calls remain in any production Go file (internal/ and cmd/)
- All 162 original http.Error calls are now apierr.Write calls with package-prefixed error codes
- Every error response logs via slog with request_id, user_id, method, path, error_code
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-error-handling/02-03-SUMMARY.md`
</output>
